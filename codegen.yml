overwrite: true
schema: ${process.env.NEXT_PUBLIC_STRAPI_GRAPHQL_ENDPOINT}
documents: "graphql/*.{gql,graphql}"
generates:
  graphql/generated/schema.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
