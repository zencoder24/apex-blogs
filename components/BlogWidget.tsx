import React from 'react';
import moment from 'moment';
import Link from 'next/link';

const BlogWidget = () => {
    return (
        <div>
            <p>Blog Widget</p>
        </div>
    );
};

export default BlogWidget;